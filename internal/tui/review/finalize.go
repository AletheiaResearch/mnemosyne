package review

func FinalizeScreen() Screen {
	return New("Finalize", "The final review step is publication approval. Publish revalidates the saved attestation record against the current file on disk.")
}
